# ═══════════════════════════════════════════════════════════════════════
# 项目三：多Agent — Python 依赖
# ═══════════════════════════════════════════════════════════════════════

# ── 核心运行时 ─────────────────────────────────────────────────────
python-dotenv>=1.0.0        # .env 配置加载（llm_scanner.py）
prometheus-client>=0.19.0   # 可观测性指标
psutil>=5.9.0               # 系统指标采集

# ── API 服务（可选，CLI 模式不需要）─────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0

# ── 语义向量检索经验（可选，不装则经验检索自动降级）──────────────
# sentence-transformers>=2.2.0   # ~700MB（含 torch）
# pymilvus>=2.4.0
# numpy>=1.24.0

# ── 测试 ────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
